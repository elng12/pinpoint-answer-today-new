# Scripts Guide

脚本现在按用途分组：

- `scripts/dev/`：本地开发辅助脚本
- `scripts/puzzles/`：拼图数据校验、内容回归测试
- `scripts/migration/`：一次性迁移或导入脚本

常用入口保持不变，继续直接用 `npm run validate:data` 和 `npm run test:pinpoint-regression` 即可。
